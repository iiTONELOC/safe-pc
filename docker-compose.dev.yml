services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: safe-pc-dev
    volumes:
      - .:/app
    network_mode: host
    environment:
      PYTHONPATH: /app/src
    command: poetry run dev
